public class Light {
  Vector pos;
  double intensity;
  Vector color;
  public Light(Vector pos, double intensity, Vector color) {
      this.pos = pos;
      this.intensity = intensity;
      this.color = color;
  }
}